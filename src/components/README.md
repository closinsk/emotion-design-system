# Components

This folder follows an Atomic pattern: `atoms/`, `molecules/`, `organisms/`, and `tokens/` for CSS variables.

Naming follows the botanical theme from the design agent: variables use plant-inspired names.
